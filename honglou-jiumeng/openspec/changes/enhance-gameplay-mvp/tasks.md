1. Core loop - 節氣行動力
   - 在狀態中新增 `actionPointsPerJieqi` 與 `currentActionPoints`。
   - 三類行動掛鉤行動力：澆灌(1)、修復(2-3)、種植(2)、尋記憶(1-2)。
   - UI：頂部或面板顯示行動力，為 0 時禁用行動按鈕並引導「推進節氣」。
   - 驗證：每節氣重置 AP；超出 AP 的行為被拒並提示。

2. 園林佈局 Buff（格子連鎖）
   - 建立 buff 設定（瀟湘館周邊水系/芙蓉→黛玉成長+X%；蘅蕪苑整齊→穩定度+；海棠社詩酒→湘雲加成）。
   - 每次建造/移除時重算受影響格子 buff。
   - UI：達成條件時在建築/花魂詳情顯示「佈局加成已生效」圖示與簡述。
   - 驗證：放置/移除導致加成變化可觀測。

3. 花魂雙路線與品質
   - 黛玉花魂根據近期淚水使用偏好，標記「極致悲情」或「柔中帶剛」走向。
   - 數值：路線影響成長曲線/共鳴值/事件觸發機率。
   - 視覺：狀態標籤與小幅外觀差異（色調/粒子）。
   - 驗證：切換淚水搭配後，路線能轉向或鎖定（依設計）。

4. 記憶清晰度系統
   - 每段記憶新增 `clarity`（0-100）；抽取/重溫↑、每節氣/輪迴自然衰減↓。
   - UI：列表以清晰度改變文本/樣式（完整段→關鍵詞→模糊影像）。
   - 驗證：長期不重溫自動衰退；重溫回暖。

5. AI 文字調味（低頻）
   - 輪迴短評：每輪最多一次；失敗降級本地文案；結果快取。
   - 記憶情緒補光：首次抽取時生成一句，快取於本地。
   - 結局總結：每局一次，輸入遊戲摘要；失敗降級本地文案。
   - 驗證：頻率/字數限制，斷網降級。

6. UI 視覺重構（第一步）
   - Tailwind 版型：頂部狀態列、右側面板、卡片式格子。
   - 替換 emoji→SVG 圖標（靈石/絳珠/記憶/花魂/鳥靈）。
   - 導入少量插畫（瀟湘館/怡紅院/蘅蕪苑/海棠社/秋爽齋、大觀園遠景）。
   - 驗證：主要界面無 emoji；插畫縮放清晰；暗色/亮色檢視。

7. 模組化與打包預留
   - `js/main.js` 拆分為 `state.js / garden.js / flowers.js / memories.js / ui-dialogs.js / ai-integration.js`。
   - 所有資源使用相對路徑並移入 `honglou-jiumeng/assets/`（如需要）。
   - 在 README 補充 Capacitor（iOS）／Electron（桌面）打包筆記。

8. 文檔與驗收
   - 更新 `GAMEPLAY_AI_PLAN.md` 的進度註記與 TODO。
   - 補充 `LIN_DAIYU_TEARS.md` 與系統映射表（淚水→效果）第一版。
   - 開發過程設計開關（feature flags）逐項驗收。


